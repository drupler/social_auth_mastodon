social_auth_mastodon.settings:
  type: config_object
  label: 'Social Auth Mastodon settings'
  mapping:
    client_id:
      type: string
      label: 'Client ID'
    client_secret:
      type: string
      label: 'Client Secret'
    scopes:
      type: string
      label: 'Scopes to be requested'
    endpoints:
      type: string
      label: 'API Endpoints to be requested'
    restricted_domain:
      type: string
      label: 'Restricted Domain'
